1) Gnosis Chain(20000) - EVM blockchain // dual-token model: xDai & GNO

2) Scroll(20000) - layer2 rollup // zkEVM, zk-rollup

3) Optimism(20000) - L2 blockchain

4) Hyperlane(10000) - interoperability layer // anyone can bring interoperability 
  anywhere, the interoperability layer purpose built for the modular blockchain world

5) UMA & Across(10000): 
  UMA - oracle    // optimistic oracle (OO), decentralized truth machine
  Across - bridge // cross-chain bridge between L2s and to L1. Across transfers are secured by UMA’s optimistic oracle

6) Filecoin Virtual Machine (FVM)(10000) - runtime environment for smart contracts on the Filecoin network

7) ApeCoin(10000) - 

8) Fuel Labs(7000) - execution layer for the modular blockchain stack // 
  parallel transaction execution and ability to go beyond the limitations of the EVM

9) Connext(5000) - modular interoperability protocol // for secure crosschain dApps(xApps)

10) zkSync(5000) - L2 scaling solution // deploy smart contracts written in Solidity on a ZK Rollup

11) Polybase(5000) - web3 replacement for Firebase and Postgres // 
  can store any kind of structured data: user data, app state and metadata. 
  Compose it with smart contracts (using oracles) and file storage (like Filecoin) for truly decentralized applications

12) Taiko(5000) - Ethereum-equivalent ZK-Rollup // security guarantees of Ethereum and lower transaction fees

13) Mantle(5000) - ... 

14) Intmax(5000) - Layer2 // Intmax protocol (+Intmax wallet) is completely stateless Layer2 with interoperability

15) Push Protocol(5000) - web3 communication protocol // cross-chain notifications and messaging for dapps, wallets and services

16) The Graph(5000) - indexing protocol // for querying networks like Ethereum, Polygon and IPFS. 
  Anyone can build and publish open APIs, called subgraphs, making data easily accessible